# Changesets

Run `bun changeset` to record a versioned change. On merge to `main`, the release workflow opens (or updates) a "Version Packages" PR. Merging that PR publishes to npm.

See https://github.com/changesets/changesets for the full docs.
